// Compliance gate (stub): scans app copy + code for employer-language violations.
// Fails the build on terms implying employment: "employee", "wages", "shift assignment",
// "we hire", "our crew" (rule M-1 in docs/COMPLIANCE.md). Wire AST/grep checks here.
console.log("classification-lint: TODO — wire employer-language checks before launch.");
